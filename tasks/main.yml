#SPDX-License-Identifier: MIT-0
---
# tasks file for traefik

- name: Ensure traefik group exists
  group:
    name: "{{ traefik_group }}"
    system: true
    state: present

- name: Ensure traefik user exists
  user:
    name: "{{ traefik_user }}"
    group: "{{ traefik_group }}"
    system: true
    shell: /usr/sbin/nologin
    home: "{{ traefik_home_dir }}"
    create_home: false
    state: present

- name: Create traefik directories
  file:
    path: "{{ item }}"
    state: directory
    owner: "{{ traefik_user }}"
    group: "{{ traefik_group }}"
    mode: '0755'
  loop:
    - "{{ traefik_home_dir }}"
    - "{{ traefik_config_dir }}"
    - "{{ traefik_certs_dir }}"
    - "{{ traefik_data_dir }}"
    - "{{ traefik_log_dir }}"

- name: Check if traefik binary exists and get version
  command: "{{ traefik_binary_path }} version"
  register: traefik_current_version
  failed_when: false
  changed_when: false

- name: Download traefik binary
  get_url:
    url: "https://github.com/traefik/traefik/releases/download/v{{ traefik_version }}/traefik_v{{ traefik_version }}_linux_{{ traefik_architecture }}.tar.gz"
    dest: "/tmp/traefik_v{{ traefik_version }}_linux_{{ traefik_architecture }}.tar.gz"
    mode: '0644'
  when: traefik_current_version.rc != 0 or traefik_version not in traefik_current_version.stdout

- name: Extract traefik binary
  unarchive:
    src: "/tmp/traefik_v{{ traefik_version }}_linux_{{ traefik_architecture }}.tar.gz"
    dest: "/tmp"
    remote_src: true
    creates: "/tmp/traefik"
  when: traefik_current_version.rc != 0 or traefik_version not in traefik_current_version.stdout

- name: Install traefik binary
  copy:
    src: "/tmp/traefik"
    dest: "{{ traefik_binary_path }}"
    owner: root
    group: root
    mode: '0755'
    remote_src: true
  when: traefik_current_version.rc != 0 or traefik_version not in traefik_current_version.stdout
  notify: restart traefik

- name: Clean up downloaded files
  file:
    path: "{{ item }}"
    state: absent
  loop:
    - "/tmp/traefik_v{{ traefik_version }}_linux_{{ traefik_architecture }}.tar.gz"
    - "/tmp/traefik"
  when: traefik_current_version.rc != 0 or traefik_version not in traefik_current_version.stdout

- name: Create traefik configuration file
  template:
    src: ../templates/template-traefik-yaml.j2
    dest: "{{ traefik_config_dir }}/traefik.yaml"
    owner: "{{ traefik_user }}"
    group: "{{ traefik_group }}"
    mode: '0644'
  notify: restart traefik

- name: Create traefik file provider directory
  file:
    path: "{{ traefik_provider_file_directory }}"
    state: directory
    owner: "{{ traefik_user }}"
    group: "{{ traefik_group }}"
    mode: '0755'
  when: traefik_provider_file_enabled

- name: Create service configuration files
  template:
    src: service.yaml.j2
    dest: "{{ traefik_provider_file_directory }}/{{ service_name }}.yaml"
    owner: "{{ traefik_user }}"
    group: "{{ traefik_group }}"
    mode: '0644'
  vars:
    service_config: "{{ service_data }}"
    service_name: "{{ service_item.key }}"
    service_data: "{{ service_item.value }}"
  loop: "{{ traefik_services | dict2items }}"
  loop_control:
    loop_var: service_item
    label: "{{ service_item.key }}"
  notify: restart traefik
  when:
    - traefik_provider_file_enabled
    - traefik_services | length > 0

- name: Create dashboard service configuration
  template:
    src: dashboard.yaml.j2
    dest: "{{ traefik_provider_file_directory }}/dashboard.yaml"
    owner: "{{ traefik_user }}"
    group: "{{ traefik_group }}"
    mode: '0644'
  notify: restart traefik
  when:
    - traefik_provider_file_enabled
    - traefik_api_dashboard

- name: Create traefik systemd service file
  template:
    src: traefik.service.j2
    dest: /etc/systemd/system/traefik.service
    owner: root
    group: root
    mode: '0644'
  notify:
    - reload systemd
    - restart traefik

- name: Configure logrotate for traefik logs
  template:
    src: traefik-logrotate.j2
    dest: /etc/logrotate.d/traefik
    owner: root
    group: root
    mode: '0644'

- name: Enable and start traefik service
  systemd:
    name: traefik
    enabled: "{{ traefik_service_enabled }}"
    state: "{{ traefik_service_state }}"
    daemon_reload: true
